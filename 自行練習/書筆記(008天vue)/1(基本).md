Vue.js:漸進式框架

指令式渲染(傳統方式)
一個指令一個動作

宣告式渲染
目標是希望以[宣告]的方式在html(模板)來表現js裡面的狀態(資料)
當狀態被更新的時候,Vue.js就會根據資料或者狀態的內容來同步更新html,將我們的資料渲染出來

Vue.js操作網頁邏輯就是將資料和狀態統一,由js的物件來維護管理

1.14???不懂
Vue.js與MVVM模式
Model(資料) View(畫面) ViewModel

1.15 元件
最小單位可以只是節點,也可以是好幾個節點組成而成的元件,甚至元件內還可有元件
每個元件都有屬於自己的模板,程式邏輯以及樣式,而整個網頁由這些元件構成起來,便成了元件樹

1.17???不懂

1.2 Vue.js核心:實體
當透過<script>標籤將Vue.js引入,此時瀏覽器會新增一個Vue的全域變數

1.22???複習

1.23(020頁)提示

<!-- 1217進度至1.2.4 -->